services:
  mexc-flip-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mexc-flip-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    command: npm start
